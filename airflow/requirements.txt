# Dependencias para la imagen de Airflow — separadas de requirements.txt.
#
# El Dockerfile instala esto junto con --constraint apuntando a las
# constraints oficiales de Airflow 2.10.5/Python 3.12. Varios paquetes que
# también usa Airflow por dentro tienen que coincidir EXACTO con lo que esa
# constraint fija, o pip no encuentra cómo resolverlo (así se rompió el
# build: pandas/numpy/python-dotenv/google-auth-oauthlib/
# google-api-python-client iban en versiones más nuevas que las del
# dashboard, y esas versiones no cumplían la constraint). El resto de los
# paquetes (gspread, dash, plotly, fpdf2, kaleido, playwright, pypdf...) no
# aparecen en el archivo de Airflow, así que se dejan igual que en
# requirements.txt.
#
# Si necesitas actualizar una versión de las que sí están fijadas por
# Airflow, revisa primero cuál exige la constraint:
#   https://raw.githubusercontent.com/apache/airflow/constraints-2.10.5/constraints-3.12.txt
gspread==6.2.1
gspread-dataframe==4.0.0
pandas==2.1.4
numpy==1.26.4
openpyxl==3.1.5
dash==4.4.1
plotly==7.0.0
fpdf2==2.8.8
kaleido==1.3.0
python-dotenv==1.0.1
playwright==1.55.0
pypdf==6.1.3
google-auth-oauthlib==1.2.1
google-api-python-client==2.160.0
